source 'https://rubygems.org'

gem 'rubygems'

# Access .env file in root
gem 'dotenv'

# Access .env file in root
# gem 'foreman'

# Use headless browser
gem 'headless'

# Create directories and nested directories easily
# Must sudo apt-get install libmagickwand-dev to use
gem 'fileutils'

# List watir dependencies in watir.gemspec
gemspec